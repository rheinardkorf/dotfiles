# Automatically generated by espanso migration tool
# Original file: user/random.yml

matches:
  - trigger: ":shrug"
    replace: "¯\\_(ツ)_/¯"
  - trigger: ":tableflip"
    replace: (╯°□°）╯︵ ┻━┻
  - trigger: ":tablefix"
    replace: ┬─┬ノ(º_ºノ)
  - trigger: ":swaggerdocs"
    replace: "svn export https://github.com/swagger-api/swagger-ui/trunk/dist api-docs"
  - trigger: ":gravatarme"
    replace: "{{gravatar_url}}"
    vars:
      - name: form1
        type: form
        params:
          layout: "Email [[email]]"
          fields:
            email:
              type: choice
              values:
                - rheinard.korf@xwp.co
                - rheinard@thekorfs.com
                - rkorf@rheiko.com.au
              default: rheinard@thekorfs.com
      - name: gravatar_url
        type: shell
        params:
          cmd: "HASH=`md5 -s $ESPANSO_FORM1_EMAIL | awk 'END{print $4}'` && echo https://www.gravatar.com/avatar/$HASH?s=285"
  - trigger: ":gravatarthem"
    replace: "{{gravatar_url}}"
    vars:
      - name: form1
        type: form
        params:
          layout: "Email [[email]]"
      - name: gravatar_url
        type: shell
        params:
          cmd: "HASH=`md5 -s $ESPANSO_FORM1_EMAIL | awk 'END{print $4}'` && echo https://www.gravatar.com/avatar/$HASH?s=285"