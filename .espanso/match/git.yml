# Automatically generated by espanso migration tool
# Original file: user/git.yml

matches:
  - trigger: ":gitdroptag"
    form: |
      git tag -d [[tag]]
      git push origin :refs/tags/[[tag]]
    form_fields:
      tag:
        default: ""
  - trigger: ":githardcheckout"
    form: |
      git checkout [[branch]]
      git clean -dff
      git submodule init
      git submodule update
    form_fields:
      branch:
        default: ""
  - trigger: ":gitorphan"
    form: |
      git checkout --orphan [[branch]]
      git rm -rf .
    form_fields:
      branch:
        default: ""
  - trigger: ":gitlog"
    replace: git log --pretty=oneline --abbrev-commit
  - trigger: ":gitauthors"
    replace: "git log --format='%aN <%aE>' | sort -u"
  - trigger: ":gitauthorfiles"
    replace: "git log --pretty=\"%H\" --author=\"Rheinard Korf\" | while read commit_hash; do git show --oneline --name-only $commit_hash | tail -n+2; done | sort | uniq"