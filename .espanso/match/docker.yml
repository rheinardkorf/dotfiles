# Automatically generated by espanso migration tool
# Original file: user/docker.yml

matches:
  - trigger: ":portainer"
    form: |
      docker run -d -p [[host_port]]:9000 -v /var/run/docker.sock:/var/run/docker.sock -v /Users/rheinardkorf/Utilities/docker/data:/data portainer/portainer
    form_fields:
      host_port:
        default: 9000
  - trigger: ":dockernames"
    replace: "docker ps --format '{{.Names}}'"